# Common configuration
service: task0

provider:
  name: aws
  runtime: python3.8
  stage: production
  region: eu-central-1

package:
  include:
    - common/**

# Serverless functions configuration
functions:
  hello_world:
    handler: handler.hello_world
    events:
      - http:
          path: hello_world
          method: get

